import { Text, View } from "react-native";

export default function RequestCard() {
  return (
    <View
      style={{
        padding: 16,
        borderRadius: 18,
        backgroundColor: "#fff",
      }}
    >
      <Text>Pedido</Text>
    </View>
  );
}
